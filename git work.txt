cd c\folder
git init
git clone https://github.com/visaginas/JavaBook      //copy with folder
git clone https://github.com/visaginas/JavaBook .     //copy without folder. files only
/ git branch - show all branches
/ git status - show status
/ git remote -v - show all remote repositories
/ git remote add <make_new_repos_short_name> <https://github.com/user/repo.git>
/ git checkout new_branch  - switch to a banch
git branch new_branch
git add .
git commit .
git push origin master
  origin -remote name (from git remote -v)
  master - branch name
